import { makeStyles } from '@mui/styles';

export default makeStyles(() => ({
  image: {
    maxWidth: '90%',
    boxShadow: '0.5em 0.5em 1em',
    objectFit: 'cover',
    borderRadius: '20px',
  },
}));
